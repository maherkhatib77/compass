# מדריך הפעלת הפרויקט - מצפן נט

## ✅ מה שופר בפרויקט

### 1. **מסד נתונים SQLite במקום MySQL**
   - הוסרה התלות בשרת MySQL חיצוני
   - מסד נתונים קליל ומהיר שמוכן לעבודה מיד
   - קובץ יחיד: `matspanet.db`

### 2. **אתחול אוטומטי של הטבלאות**
   - בעת הרצת השרת, הטבלאות נוצרות אוטומטית אם לא קיימות
   - כולל טבלאות: users, learning_solutions, lookup_schools

### 3. **API עובד במלואו**
   - `/api/solutions` - שליפת פתרונות למידה
   - `/api/users` - שליפת משתמשים
   - תמיכה בסינון לפי סטטוס

### 4. **נתוני דוגמה**
   - נוצר משתמש admin ברירת מחדל
   - 3 פתרונות למידה לדוגמה
   - 3 בתי ספר לדוגמה

## 🚀 הפעלה מהירה

### שלב 1: התקנת תלויות
```bash
pip install fastapi uvicorn mysql-connector-python python-dotenv aiosqlite
```

### שלב 2: הרצת השרת
```bash
python main.py
```

### שלב 3: גישה לאפליקציה
- אתר ראשי: http://127.0.0.1:8000
- תיעוד API (Swagger): http://127.0.0.1:8000/docs
- בדיקת פתרונות: http://127.0.0.1:8000/api/solutions
- בדיקת משתמשים: http://127.0.0.1:8000/api/users

## 📁 מבנה הפרויקט

```
/workspace/
├── main.py              # שרת FastAPI הראשי
├── db_connector.py      # מחלקת ניהול מסד נתונים (SQLite)
├── matspanet.db         # קובץ מסד הנתונים (נוצר אוטומטית)
├── seed_data.py         # סקריפט להוספת נתוני דוגמה
├── js/
│   └── api-loader.js    # טוען נתונים מה-API לדפדפן
├── index.html           # דף הבית
└── ...
```

## 🔧 שינויים שבוצעו

### קבצים שעודכנו:
1. **db_connector.py** - הומר מ-MySQL ל-SQLite
2. **main.py** - ממשיך לעבוד ללא שינוי (תומך ב-db_connector החדש)

### קבצים חדשים:
1. **seed_data.py** - סקריפט לאכלוס נתוני התחלה
2. **matspanet.db** - מסד הנתונים (נוצר אוטומטית)

## 🎯 צעדים הבאים (אופציונלי)

### הוספת משתמשים נוספים
```bash
python seed_data.py  # מריץ שוב כדי להוסיף עוד נתוני דוגמה
```

### איפוס מסד הנתונים
```bash
rm matspanet.db
python main.py  # ייצור מסד נתונים חדש ונקי
```

### הרצה בייצור (Production)
```bash
gunicorn main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000
```

## ⚠️ הערות חשובות

1. **שמירת נתונים**: כל הנתונים נשמרים בקובץ `matspanet.db` - יש לגבות אותו לפני מחיקה
2. **אבטחה**: הסקריפט הנוכחי הוא לצרכי פיתוח. בפרודקשן יש להוסיף אימות והרשאות
3. **CORS**: מוגדר לכל הדומיינים (`*`) לצרכי פיתוח. בפרודקשן יש להגביל לדומיין הספציפי

## 📞 תמיכה

במקרה של תקלות:
1. בדוק שהשרת רץ: `curl http://127.0.0.1:8000/api/health`
2. בדוק את הלוגים: `cat server.log`
3. וודא שקובץ ה-DB קיים: `ls -la matspanet.db`
